from setuptools import setup

setup(
    name="peaky",
    version="0.0.1",
    author="Jason M. Hite",
    license="BSD",
    packages=["peaky"],
    install_requires=['pandas', 'numpy', 'scipy'],
)
